# Core dependencies used by ETL, constraints, and evaluation code paths.
# Safe for the server image and the Colab LLM image alike.

# Graph database driver
neo4j>=5.0.0,<6.0.0

# IFC parsing
ifcopenshell>=0.7.0

# Configuration management
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Env / misc
python-dotenv>=1.0.0
numpy

# API server (exposes Neo4j to Colab behind auth)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# Colab-side HTTP client for the API (also pulled in by FastAPI's TestClient)
httpx>=0.27
